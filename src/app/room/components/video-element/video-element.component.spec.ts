import { VideoElementComponent } from './video-element.component';

describe('VideoElementComponent', () => {
  it('should create an instance', () => {
    const directive = new VideoElementComponent();
    expect(directive).toBeTruthy();
  });
});
